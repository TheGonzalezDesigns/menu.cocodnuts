{
	"scripts": {
		"start": "npm run git && npm run client && npm run server",
		"client": "cd client && npm run init && cd .. && cp client/dist/* ./ && clear",
		"server": "cd server && npm run init && cd .. && clear",
		"git": "git fetch && git pull origin master"
	}
}
