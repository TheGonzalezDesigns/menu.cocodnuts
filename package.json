{
	"scripts": {
		"start": "npm run git && clear && npm run client && clear && npm run server && clear",
		"client": "cd client && npm run init && cd .. && cp client/dist/* ./",
		"server": "cd server && npm run init && cd ..",
		"git": "git fetch && git pull origin master"
	}
}
